@using Microsoft.AspNetCore.Identity
@model IdentityServer.OpenIddict.ViewModels.Account.LoginViewModel
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager;
@{
    Layout = "_LoginLayout";
    
    var loginProviders = (await SignInManager.GetExternalAuthenticationSchemesAsync()).ToList();
}
<div class="loginav">
    <div class="video">
        <video src="~/video/login-bg.mp4" muted autoplay loop></video>
    </div>
    <div></div>
    <div class="loginbox boxall">
        <form asp-controller="Account" asp-action="Login" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">
            <div class="logo">
                <h1>身份认证中心</h1>
                <h2>SHEN FEN REN ZHEN ZHONG XIN</h2>
            </div>
            <div class="logintit">用户登录</div>
            @if (!ViewContext.ModelState.IsValid)
            {
                <div class="col-12">
                    <div asp-validation-summary="ModelOnly"></div>
                </div>
            }
            <ul class="logininput">
                <li>
                    <i>
                        <img src="~/images/user.png" alt="">
                    </i>
                    <input asp-for="UserName" type="text" value="bob@le-magnifique.com" class="forminput" placeholder="用户名" autofocus>
                </li>
                <span asp-validation-for="UserName" class="text-danger"></span>
                <li>
                    <i>
                        <img src="~/images/password.png" alt="">
                    </i>
                    <input asp-for="Password" type="password" value="}s>EWG@f4g;_v7nB" class="forminput" placeholder="密码">
                </li>
                <span asp-validation-for="Password" class="text-danger"></span>
                <div class="flex1" style="padding: 15px 0;">
                    <label asp-for="RememberMe">
                        <input asp-for="RememberMe" type="checkbox" checked class="aui-checkbox">
                        &nbsp;记住密码
                    </label>
                    <a href="#" class="text-primary">忘记密码？</a>
                </div>
                <li>
                    <button type="submit" class="btn btnblock btn-lg btn-block btn-primary w-100" tabindex="4">登录</button>
                </li>
            </ul>
            <div class="boxfoot"></div>
        </form>
    </div>
</div>